// Template (modelo) string ou template literal 
// São textos que preocessam informações
// de natureza distinta numa mesma sentença
// Ex. 'Minha mãe é `${NomeDaMaee}`
// Observe a estrutura padronizada com `${}`

const nome = 'Kauan'
const sobrenome = 'Pereira'
const perfil = `Olá, ${nome} ${sobrenome}`
console.log(perfil)